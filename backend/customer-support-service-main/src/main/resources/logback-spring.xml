<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Base directory for logs -->
    <property name="LOG_PATH" value="logs"/>
    <property name="LOG_FILE" value="customer-support-service"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [CorrId:%X{correlationId:-N/A}] [TraceId:%X{traceId:-N/A}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/${LOG_FILE}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [CorrId:%X{correlationId:-N/A}] [TraceId:%X{traceId:-N/A}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Profile-specific configurations -->
    
    <!-- Local / Default Profile: Verbose Console + File -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="in.mapmytour" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
    </springProfile>

    <!-- Production Profile: File Intensive + Minimal Console Overhead -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <!-- Limited console output to avoid Docker/Container performance issues -->
            <appender-ref ref="CONSOLE"/>
        </root>
        
        <!-- Filter CONSOLE in prod to only show ERRORs to keep it fast -->
        <appender name="CONSOLE_PROD" class="ch.qos.logback.core.ConsoleAppender">
             <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="in.mapmytour" level="INFO"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

</configuration>
