# ============================================
# NEVER commit these — secrets and live data
# ============================================

# Environment files with secrets
.env
.env.local
.env.dev
.env.prod
.env.production

# Live database data — real PostgreSQL and Redis files
data/
data/postgres_dev/
data/postgres_prod/
data/redis_dev/
data/redis_prod/

# Backup files — too large for git
backups/
*.tar.gz
complete_backup_*.tar.gz

# SSL certificates
ssl/*.pem
ssl/*.crt
ssl/*.key

# Logs
logs/
*.log

# Config file with plaintext secrets (delete this file)
config.txt

# OS files
.DS_Store
Thumbs.db
